Drop a satellite/aerial background image here named:

    satellite.jpg

A wide, high-resolution city aerial works best (e.g. a 4K satellite screenshot).
The UI loads /static/satellite.jpg automatically; if it's missing, the page
falls back to a clean dark gradient — so the app works either way.
